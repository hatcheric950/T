# Senders Hermes is permitted to reply to. Everything else is logged-only.
# Wildcards (fnmatch) allowed in emails, e.g. "*@trusted-domain.com".
# Phones must be E.164, e.g. "+14155551234".
emails: []
phones: []
